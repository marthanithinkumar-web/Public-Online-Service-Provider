import React,{useState} from 'react'
import {Link,useLocation,useNavigate} from 'react-router-dom'
import {login,verifyAdmin2FA} from '../../services/auth'
export default function AdminLogin(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [challenge,setChallenge]=useState('');const [code,setCode]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const nav=useNavigate();const location=useLocation();const requestedReturn=new URLSearchParams(location.search).get('returnTo');const safeReturn=requestedReturn&&requestedReturn.startsWith('/admin/')&&!requestedReturn.startsWith('//')?requestedReturn:'/admin/dashboard';const submit=async(e:React.FormEvent)=>{e.preventDefault();if(busy)return;setError('');setBusy(true);try{const res=challenge?await verifyAdmin2FA(challenge,code):await login(email,password);if(res?.requires_2fa){setChallenge(res.challenge_token);return}if(res?.user?.is_admin)nav(safeReturn,{replace:true});else setError('This account does not have administrator access.')}catch(err:any){setError(err?.code==='ECONNABORTED'?'The secure server took too long to respond. Please try again.':err?.response?.data?.error||'Unable to sign in. Please check your details.')}finally{setBusy(false)}};return <div className="auth-page"><div className="auth-card"><div className="auth-intro"><span className="eyebrow">Administration</span><h1>{challenge?'Verify your sign-in':'Admin sign in'}</h1><p>{challenge?'Enter the six-digit code sent to your administrator email.':'Secure access to requests, services, grievances and citizen reviews.'}</p></div><form onSubmit={submit} className="auth-form">{challenge?<label>Verification code<input inputMode="numeric" pattern="[0-9]{6}" maxLength={6} autoComplete="one-time-code" required value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,''))}/></label>:<><label>Email address<input type="email" autoComplete="username" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/></label></>}{error&&<p className="info" role="alert">{error}</p>}{busy&&<p className="auth-hint" role="status">Connecting securely… The server may take a few seconds on the first visit.</p>}<button type="submit" disabled={busy}>{busy?'Checking…':challenge?'Verify and sign in':'Sign in securely'}</button>{challenge&&<button type="button" className="btn-secondary" onClick={()=>{setChallenge('');setCode('');setError('')}}>Start again</button>}</form><div className="auth-footer"><Link to="/">← Back to public site</Link></div></div></div>}
